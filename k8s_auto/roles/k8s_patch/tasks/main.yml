---
- name: Patch Kubernetes static pod resources and arguments
  include_tasks: update_resources.yml
  loop: "{{ k8s_static_pod_patches }}"
  when:
    - "'master' in group_names"
    - "'k8s_patch' in k8s_install_services"
  tags: patch
