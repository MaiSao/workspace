apiVersion: v1
kind: Service
metadata:
  name: snmp-switch-exporter-nodeport-svc
  namespace: kube-system
spec:
  ports:
  - nodePort: 9217
    port: 9216
    protocol: TCP
    targetPort: 9216
  selector:
    app: snmp-switch-exporter
  type: NodePort
